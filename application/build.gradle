dependencies {
    compile "org.springframework.boot:spring-boot-starter-web"
    compile project(":fleet")
    compile project(":rental")

    testCompile "org.springframework.boot:spring-boot-starter-test:${springBootVersion}"
}